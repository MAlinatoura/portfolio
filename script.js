const button=document.querySelector(".menu-button");const nav=document.querySelector("#navigation");if(button&&nav){button.addEventListener("click",()=>{const expanded=button.getAttribute("aria-expanded")==="true";button.setAttribute("aria-expanded",String(!expanded));nav.classList.toggle("open",!expanded)});nav.querySelectorAll("a").forEach(link=>link.addEventListener("click",()=>{button.setAttribute("aria-expanded","false");nav.classList.remove("open")}));}const careerCards=[...document.querySelectorAll("[data-career-card]")],careerDots=document.querySelector(".career-dots"),previous=document.querySelector("[data-career-prev]"),next=document.querySelector("[data-career-next]");let activeCareer=0;if(careerCards.length&&careerDots){careerCards.forEach((card,index)=>{const dot=document.createElement("button");dot.type="button";dot.className="career-dot";dot.setAttribute("role","tab");dot.setAttribute("aria-label","Station "+(index+1)+": "+card.querySelector("h3").textContent.trim());dot.addEventListener("click",()=>setCareer(index,true));careerDots.append(dot);card.addEventListener("click",()=>setCareer(index,false));});const dots=[...careerDots.children];function setCareer(index,moveFocus){activeCareer=(index+careerCards.length)%careerCards.length;careerCards.forEach((card,i)=>{const distance=(i-activeCareer+careerCards.length)%careerCards.length;card.classList.toggle("is-active",i===activeCareer);card.classList.toggle("is-after",distance===1);card.classList.toggle("is-before",distance===careerCards.length-1);card.tabIndex=i===activeCareer?0:-1;});dots.forEach((dot,i)=>dot.setAttribute("aria-selected",String(i===activeCareer)));if(moveFocus)careerCards[activeCareer].focus({preventScroll:true});}previous?.addEventListener("click",()=>setCareer(activeCareer-1,true));next?.addEventListener("click",()=>setCareer(activeCareer+1,true));document.querySelector(".career-carousel")?.addEventListener("keydown",event=>{if(event.key==="ArrowLeft"){event.preventDefault();setCareer(activeCareer-1,true)}if(event.key==="ArrowRight"){event.preventDefault();setCareer(activeCareer+1,true)}});setCareer(0,false);}